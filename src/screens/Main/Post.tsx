import React from "react";
import { Text, View } from "react-native";
import Layout from "../../components/Layout";

const Post: React.FC<{}> = ({}) => {
  return (
    <Layout>
      <Text>Posts</Text>
    </Layout>
  );
};

export default Post;
